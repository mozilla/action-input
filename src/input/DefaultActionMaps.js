/**
 * Flat mode keyboard map for text input
 */
const KeyboardFlatTextInput = [1, 2, 3];

/**
 * Portal mode keyboard map for text input
 */
const KeyboardPortalTextInput = [4, 5, 6];

/**
 * Immersive mode keyboard map for text input
 */
const KeyboardImmersiveTextInput = [7, 8, 9];

/**
 * Flat mode Vive controller map for text input
 */
const ViveControllerFlatLocomotion = [10, 11, 12];

export { KeyboardFlatTextInput, KeyboardPortalTextInput, KeyboardImmersiveTextInput, ViveControllerFlatLocomotion };
