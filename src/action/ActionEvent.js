import Device from "../hardware/Device.js";

/**
 *	ActionEvent indicates that an {@link Action} state has changed.
 *
 *	ActionEvents refer to the source hardware {@link Device} so that applications can give the user device specific feedback.
 */
export default class ActionEvent {}
